import {InitialStateType} from "./users-reducer";

test("", () => {
    const state: InitialStateType = {
        list: [],
        pageSize: 20,
        totalUsersCount: 0,
        currentPage: 1,
        isFetching: false,
        isFollowing: []
    }

    //expect()
});
